---
# Duplicate this file, rename it to your page's slug, and fill in the fields below.
# draft: true keeps it out of production (see isVisible() in src/lib/content.ts)
# but still visible when running `astro dev`, so you can preview it locally.
title: 'Page Title'
description: 'One or two sentences for the meta description.'
# updatedDate: 2026-01-02 # optional; only add once you actually revise the page
order: 99 # lower numbers sort first where pages are listed; adjust to place it
draft: true # ⭐ flip to false (or remove) when ready to publish
---

Write the page content here. Markdown or MDX (rename to `.mdx` for components).
